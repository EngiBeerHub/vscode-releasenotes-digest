{
  "name": "release-digest",
  "icon": "assets/icon.png",
  "displayName": "Release Digest",
  "description": "Quickly catch the important changes in frequent VS Code release notes with a concise Japanese AI digest.",
  "version": "0.1.0",
  "publisher": "Engibeer",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/EngiBeerHub/vscode-releasenotes-digest.git"
  },
  "engines": {
    "vscode": "^1.91.0"
  },
  "categories": [
    "Other",
    "Machine Learning"
  ],
  "keywords": [
    "release notes",
    "vscode updates",
    "release digest",
    "ai summary",
    "summary",
    "japanese",
    "copilot"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "releaseDigest.summarizeCurrentRelease",
        "title": "Release Digest: 現在のVS Code Release Notesを要約"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -watch -p tsconfig.json",
    "lint": "eslint src",
    "test": "npm run build && node --test out/test/**/*.test.js",
    "check": "npm run lint && npm test",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.91.0",
    "@vscode/vsce": "^3.6.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0"
  }
}
